@model IEnumerable<string>
    
@foreach (var cat in Model)
{
    @Html.RouteLink(cat, new
        {
            controller = "Product",
            action = "List",
            category = cat,
            page = 1
        },
        new
        {
            @class = cat == ViewBag.Category ? "selected" : null
        })
}

